export enum STATUS {
	SUCCESS = 200,
	CREATED = 200,
	INTERVAL_SERVER_ERROR = 500,
	UNAUTHORIZED = 401,
}

export enum CODE {
	SUCCESS = 1,
	ERROR = 2,
	UNAUTHORIZED = 3,
}

export enum MESSAGE {
	SUCCESS = "Success",
	ERROR = "Error",
	UNAUTHORIZED = "Unauthorized",
}
